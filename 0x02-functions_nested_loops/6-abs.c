#include "main.h"
/**
 * main - check the code
 * computes the absolute value of an integer.
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
