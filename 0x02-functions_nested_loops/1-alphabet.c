#include "main.h"
/**
 * print_alphabet - utilizes on the _putchar function to print the alphabet a - z.
 */
void print_alphabet(void)
{
	int ch;
	for (ch =0; ch <= 8; ch++)
		_putchar(ch);
	_putchar('\n');
}
